"""
梦境正文 AI 提示词模块

职责：辅助用户把零散、模糊的梦境素材写成清晰、有画面感、可读性好的梦境正文（私人记录口吻，非解梦报告、非小说旁白）。
"""

# ---------------------------------------------------------------------------
# 「指令理解与深化」：把简短意图写成可执行的修改说明（不限定题材或风格标签）
# ---------------------------------------------------------------------------
OPTIMIZE_INSTRUCTION_PROMPT = """用户在为梦境生成写的「补充指令」往往简短或口语化。请改写为**清晰、可执行**的修改意图（如氛围、感官重点、节奏、场景或情节调整），便于下一步正文模型落实。
语言：{target_language}。篇幅与原文相近或略长，不要过度发挥。
只输出改写后的指令文本本身，不要引号、标题或解释。

原文：
---
{text}
---
"""

# ---------------------------------------------------------------------------
# 三种模式：一句点明侧重点（能力矩阵在总模板中已内化工作流，此处避免重复长段说明）
# ---------------------------------------------------------------------------
CONTENT_ASSIST_TASK_IMAGERY = (
    "意象补完：针对短语或零散关键词，按梦境逻辑补出一段可嵌入正文的描写，包含合理感官细节与情节走向；不做解梦。"
)

CONTENT_ASSIST_TASK_LITERARY = (
    "叙述润色：在保留事件与情绪的前提下，把直白、口语化的句子改得更具体、有画面感与节奏；语气由现有正文自然延伸。"
)

CONTENT_ASSIST_TASK_CONTINUE = (
    "智能续写：沿前文脉络推演后续，可加入梦境式非理性转折；严禁复述前文原句；人称与语气与前文一致。"
)

_CONTENT_ASSIST_TASK_BY_ACTION: dict[str, str] = {
    "imagery_completion": CONTENT_ASSIST_TASK_IMAGERY,
    "literary_polish": CONTENT_ASSIST_TASK_LITERARY,
    "smart_continue": CONTENT_ASSIST_TASK_CONTINUE,
}


def get_content_assist_task_detail(action: str) -> str:
    """根据 assist-content 接口的 action 返回「模式」段落。"""
    try:
        return _CONTENT_ASSIST_TASK_BY_ACTION[action]
    except KeyError as e:
        raise ValueError(f"unknown content assist action: {action}") from e


# ---------------------------------------------------------------------------
# 正文生成主模板（意象补完 / 叙述润色 / 智能续写）
# ---------------------------------------------------------------------------
DREAM_CONTENT_ASSIST_PROMPT = """你的职责：把用户碎片化、模糊的梦境记录写成**清晰、有画面感、读起来顺畅**的正文，口吻像私人梦境日记，不要解梦腔、不要小说连载旁白。输出语言必须为 {target_language}。

【梦境正文】
---
{content}
---

【用户指令】
---
{instruction}
---

【当前模式】
{task_detail}

内化工作流（不要在输出中写出分析或步骤名称）：
1. **输入判断**：依【当前模式】区分关键词扩写、整段润色或续写下文。
2. **氛围锚定**：从【梦境正文】把握整体情绪基调，贯穿生成结果；勿用外来标签强行改调。
3. **生成**：润色时加强感官细节与叙述节奏；续写时人称一致，可出现梦境式跳跃或非理性转折，结尾可留白或平稳收束。
4. **补充指令**：与模式冲突时，以用户明确指令为准。

输出规范：
- **仅输出**生成后的梦境正文；不得附带前言、标题、列表、解说或套话。
- 正文长度**不超过 3000 字**（含标点与空格）；若超出，请压缩表达、合并重复段落与细节，保留最关键的画面与情绪。
- 禁止色情、煽动暴力、自伤等不安全内容。
"""
