---
alwaysApply: true
---

# Dream Log 项目开发规则

## 1. 概览

这是一个前后端分离的 Monorepo 项目，旨在记录、分析和解析梦境。

- **`backend/`**: Django 后端服务。
- **`frontend/`**: React 前端应用。
- **`docker-compose.yml`**: 用于本地开发和生产环境的容器编排。
- **`nginx/`**: Nginx 反向代理配置。

---

## 2. 后端 (Backend - Django)

### 核心技术栈
- **框架**: Django 4.2, Django REST Framework (DRF)
- **语言**: Python 3.9
- **异步任务**: Celery

---

## 3. 前端 (Frontend - React)

### 核心技术栈
- **框架/构建**: React19, Vite
- **语言**: JavaScript (JSX)
- **UI 组件库**: `shadcn/ui`
- **状态管理**: React Context API
- **路由**: React Router


## 部署
- 项目使用 Docker 和 `docker-compose.yml`和deploy.yml 进行容器化自动化CICD部署。`Dockerfile` 分别位于 `backend/` 和 `frontend/` 目录中。

# 项目通用开发规范
## 1. 项目结构规范
目录结构组织原则
- 保持项目结构清晰，遵循模块化原则
- 相关功能应放在同一目录下
- 使用具描述性的目录命名，准确反映内容

## 2.代码规范
代码质量原则
- 遵循 SOLID 设计原则
- 避免重复代码（DRY 原则）
- 保持代码简洁、清晰、易读
- 注重可维护性与可扩展性

依赖管理
- 使用官方包管理工具（如 npm, pip 等）
- 锁定依赖版本，确保构建一致性
- 定期更新依赖，修复安全隐患
- 优先使用稳定库，避免重复造轮子
